name: motion_segment
description: A highly customizable animated segmented button for Flutter with smooth sliding indicator animations.
version: 0.1.3
homepage: https://github.com/reginhelpsyou-pixel/flutter-motion_segment
repository: https://github.com/reginhelpsyou-pixel/flutter-motion_segment
issue_tracker: https://github.com/reginhelpsyou-pixel/flutter-motion_segment/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
